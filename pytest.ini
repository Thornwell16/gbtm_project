[pytest]
testpaths = tests
addopts = -v --tb=short
timeout = 300
markers =
    slow: tests that run many model fits (deselect with -m "not slow")
    recovery: parameter-recovery tests against simulated ground truth
